apiVersion: v1
kind: Pod
metadata:
  name: traffic
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: traffic
      image: curlimages/curl:8.17.0
      command:
        - sh
        - -c
        - 'while true; do curl -fsS http://sample-http/ >/dev/null; sleep 0.1; done'
