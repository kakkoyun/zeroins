apiVersion: v1
kind: Pod
metadata:
  name: cpu-burn
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: cpu-burn
      image: busybox:1.37
      command:
        - sh
        - -c
        - 'i=0; while [ "$i" -lt 10000000 ]; do i=$((i+1)); done'
